name: gmailtest
description: gmailtest_app

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: "^3.0.2"

  cupertino_icons: ^0.1.0
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/google-g-logo.png
